/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // App Router under src/app; everything else lives under src/ too.
  experimental: {},
};

module.exports = nextConfig;