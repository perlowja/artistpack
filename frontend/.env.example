# Local development API base URL. The backend runs on port 8000 by default;
# Task 7 of docs/mvp-plan.md owns that surface. Set this to a real backend
# URL to override.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1